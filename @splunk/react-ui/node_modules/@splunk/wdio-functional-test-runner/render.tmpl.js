'use strict';

var _react = require('react');

var _reactDom = require('react-dom');

/* global document, fixture */

// This template works in combination with react-render-loader, which will provide 'fixture'

(0, _reactDom.render)((0, _react.createElement)(fixture), document.getElementById('test-container'));